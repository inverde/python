# repositories/__init__.py
# Makes 'repositories' a package
from survey.repositories import (
    survey_repo,
    region_repo,
    district_repo,
    school_repo,
    building_repo,
    respondent_repo,
    section_repo
)
